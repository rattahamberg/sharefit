To build and run:

Start frontend:

`cd frontend/sharefit`

`pnpm run dev`

Start backend:
(from root)

`cd backend`

`pnpm run dev`

Go to the link in the frontend console

Database wont work on unauthorized IPs